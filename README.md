# review-manager

Automatically assign reviews to new pull requests and on `r?` comments.

## Setup

```sh
# Install dependencies
npm install

# Run typescript
npm run build

# Run the bot
npm start
```

